---
interface Props {
  study?: string;
  finding?: string;
}

const { study, finding } = Astro.props;
---

<div class="my-6 p-5 bg-slate-50 border border-slate-300 rounded-lg">
  {study && <p class="text-sm font-semibold text-slate-700 mb-2">📊 {study}</p>}
  {finding && <p class="text-base text-gray-800">{finding}</p>}
  <slot />
</div>
