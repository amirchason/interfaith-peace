---
interface Props {
  title?: string;
  duration?: string;
}

const { title, duration } = Astro.props;
---

<div class="my-8 p-6 bg-purple-50 border-l-4 border-purple-500 rounded-r-lg">
  {title && <h4 class="text-lg font-bold text-purple-900 mb-2">🧘 {title}</h4>}
  {duration && <p class="text-sm text-purple-700 mb-4">Duration: {duration}</p>}
  <div class="prose prose-purple max-w-none text-gray-800">
    <slot />
  </div>
</div>
