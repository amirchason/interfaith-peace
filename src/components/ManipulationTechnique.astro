---
interface Props {
  technique?: string;
  example?: string;
}

const { technique, example } = Astro.props;
---

<div class="my-4 p-4 bg-red-50 border border-red-200 rounded-lg">
  {technique && <h5 class="text-base font-bold text-red-900 mb-2">🚨 {technique}</h5>}
  {example && <p class="text-sm text-gray-800">{example}</p>}
  <slot />
</div>
